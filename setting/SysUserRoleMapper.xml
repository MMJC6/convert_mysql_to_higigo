<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.starso.job.modules.setting.mapper.SysUserRoleMapper">

	<resultMap type="com.starso.system.api.domain.TbSysUserRole" id="SysUserRoleResult">
		<result property="userId"     column="user_id"      />
		<result property="roleId"     column="role_id"      />
	</resultMap>

	<delete id="deleteUserRoleByUserId" parameterType="java.lang.String">
		delete from sys_user_role where user_id=#{userId}
	</delete>

	<select id="countUserRoleByRoleId" resultType="int" parameterType="java.lang.String">
	    select count(1) from sys_user_role where role_id=#{roleId}
	</select>

	<delete id="deleteUserRole" parameterType="java.lang.String">
 		delete from sys_user_role where user_id in
 		<foreach collection="array" item="userId" open="(" separator="," close=")">
 			#{userId}
        </foreach>
 	</delete>

	<insert id="batchUserRole">
		insert into sys_user_role(user_id, role_id) values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.userId},#{item.roleId})
		</foreach>
	</insert>

    <insert id="insertUserRole" parameterType="com.starso.system.api.domain.TbSysUserRole">
		insert into sys_user_role(user_id, role_id) values (#{userId,jdbcType=VARCHAR},#{roleId,jdbcType=VARCHAR})
	</insert>

    <delete id="deleteUserRoleInfo" parameterType="com.starso.system.api.domain.TbSysUserRole">
		delete from sys_user_role where user_id=#{userId} and role_id=#{roleId}
	</delete>

	<delete id="deleteUserRoleInfos">
	    delete from sys_user_role where role_id=#{roleId} and user_id in
 	    <foreach collection="userIds" item="userId" open="(" separator="," close=")">
 	        #{userId}
            </foreach>
	</delete>

	<delete id="deleteUserRoleByArryUserId">
		delete from sys_user_role where 1=1 and user_id in
		<foreach collection="deleteSysUseRoleList" item="item" open="(" separator="," close=")">
			#{item.userId}
		</foreach>
	</delete>
</mapper>
